// Write a function min that takes to arguments and return their minimum

function minimum (number1, number2) {
    if (number1 < number2) {
        return number1;
    } else {
        return number2; 
    }
}